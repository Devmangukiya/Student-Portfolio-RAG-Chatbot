venv/
.venv/
pycache/
*.pyc
*.pyo
*.pyd
.env
data/
vectorstore/
eval/
.vscode/
.idea/
.DS_Store
SIH.egg-info/


### Your Final, Correct Workflow

# 1.  **Create the `.gitignore` file** in your root directory with the content above.
# 2.  **Remove the unwanted files from Git's tracking:** (You only need to do this once).
#     ```bash
#     git rm -r --cached venv __pycache__ data vectorstore eval .env
#     git add .gitignore
#     git commit -m "Add .gitignore and remove transient files"
#     ```
# 3.  **Pull the remote changes:**
#     ```bash
#     git pull origin main
#     ```
# 4.  **Push your final, clean project:**
#     ```bash
#     git push origin main
